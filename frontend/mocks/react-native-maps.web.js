// Mock react-native-maps for web - this file is never actually used
// but prevents Metro from trying to bundle the real react-native-maps on web
export default null;
export const Marker = null;
export const PROVIDER_DEFAULT = null;
